node_modules/
out/
*.log
.DS_Store
.env
.env.*
# Fixture content for the git-map scan engine's tests is meant to be
# committed, including a sanitised .env.example (see src/scan/safe-provider.js
# and tests/fixtures/repos/next-supabase-stripe) -- carve it back out of the
# blanket .env.* ignore above.
!tests/fixtures/repos/**/.env.example
evals/results/

# Isolated worktrees created for background agents; never part of the repo.
.claude/worktrees/
.playwright-mcp/
