// This repository's `examples/` directory IS its product: the root
// manifest names it as both the entry point and a workspace. The
// relevance policy must therefore keep it.
const stripe = require('stripe');

module.exports = { stripe };
