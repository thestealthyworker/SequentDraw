[package]
name = "billing"
version = "0.1.0"
license = "MIT"

[dependencies]
serde = "1.0"
tokio = { version = "1.36", features = ["full"] }
stripe-rust = { git = "https://github.com/arlyon/async-stripe", branch = "main" }
shared = { path = "../shared" }
sqlx = { version = "0.7" , default-features = false }

[dev-dependencies]
mockall = "0.12"

[build-dependencies]
cc = "1.0"
