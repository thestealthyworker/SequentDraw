---
type: tool_used
tool: Skill
input_match: git-map
min: 1
weight: 2
arm: with-only
---

The git-map skill should fire for a request to diagram a repository given by
a GitHub URL. Output graders (evidence, artifact publish) come in the
follow-up once the scanner (src/scan/) is merged -- for now this only grades
that the skill fires.
