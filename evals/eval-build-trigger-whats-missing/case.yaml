schema_version: "1.1"
name: eval-build-trigger-whats-missing
# Every eval run starts in an EMPTY workspace, so the booking map this prompt
# names does not exist unless it is scaffolded in. The fixture is a small
# cleaning-business map committed beside this case (a business map, unlike
# the Medusa example the other review cases use).
# The script only runs when the suite is invoked with --scaffold.
context:
  scaffold_script: fixture.sh
