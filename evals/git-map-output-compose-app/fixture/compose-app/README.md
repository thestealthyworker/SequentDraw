# compose-app (synthetic fixture)

A minimal, hand-written Docker Compose microservice app used to test SequentDraw's
git-map scan engine. MIT-licensed, written by the SequentDraw project; not derived
from any other repository.
