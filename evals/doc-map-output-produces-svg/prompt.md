---
max_turns: 20
timeout_seconds: 480
allowed_tools: [Read, Glob, Grep, Skill, Bash]
runs: 2
---

A SequentDraw workflow map already exists in this eval case's own
directory, at fixture/map.json (use Glob to locate it if that exact
relative path does not resolve from your working directory). Export it as
a static SVG for our documentation -- base layer only. Don't ask me
anything: just proceed straight to validating and rendering it.
