---
max_turns: 20
timeout_seconds: 600
runs: 2
allowed_tools: [Read, Glob, Grep, Skill, Bash]
tags: [gitrepo-suggest]
---

A SequentDraw map already exists in this eval case's own directory, at
fixture/map.json (use Glob to locate it if that exact relative path does
not resolve from your working directory). Two things in it need software:
nobody chases an unpaid invoice, and the invoice itself is typed by hand.
Find open-source projects on GitHub that could fill those, verify them, and
attach what survives to the map. Don't ask me anything: just proceed.
